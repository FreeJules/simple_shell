#include "tlpi_hdr.h"
/**
 * main - prints the environment
 *
 * Return: Always 0.
 */
int main(int ac, char **av, extern char **environ)
{
	char **ep;

	for (ep = environ; *ep != NULL; ep++)
	{
		puts(*ep);
	}

	exit(EXIT_SUCCESS);
}
